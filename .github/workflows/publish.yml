name: Publish to npm

on:
  push:
    branches:
      - master
    paths:
      - "package.json"

permissions:
  contents: write
  id-token: write

jobs:
  release:
    uses: artkrsk/github-actions/.github/workflows/npm-publish.yml@v1
