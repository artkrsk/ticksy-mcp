name: Publish to npm

on:
  push:
    branches:
      - master
    paths:
      - "package.json"

jobs:
  release:
    uses: artkrsk/github-actions/.github/workflows/npm-publish.yml@v1
    permissions:
      contents: write
      id-token: write
